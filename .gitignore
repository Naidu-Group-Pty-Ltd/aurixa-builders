node_modules/
.DS_Store
*.log
dist/
*.tsbuildinfo

# Deno resolves the acceptance harness's npm imports from its own global cache
# (`--node-modules-dir=none`), so this lock is a local artefact of running the
# gate, not a dependency of the product.
deno.lock
__pycache__/
